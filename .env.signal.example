# Optional: manual bind (otherwise server uses PORT or default 0.0.0.0:8080)
BIND_ADDR=0.0.0.0:8080

# Required for production. If set, all endpoints except /health require x-nexus-token header.
SIGNAL_API_TOKEN=change-me-strong-token
